import React from 'react';

const FormAction = () => (
    <footer className="form-actions">
        <button className="generate-challan-btn">GENERATE CHALLAN</button>
    </footer>
);

export default FormAction;